# Test plan

Unit coverage includes canonical argument equality, duplicate blocking, total attempt cap, per-tool cap, Russian stop and loop detection, malformed non-array user content, and turn reset.

Syntax checks validate both ESM source files. Runtime integration and deployment smoke tests are deferred until deployment is approved.
